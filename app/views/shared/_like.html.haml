//①
-# - if user_signed_in?
-#   - if @book.like_user(current_user.id)
-#     = button_to book_like_path(like, book_id: book.id), method: :delete, id: "like-button", remote: true do
-#       = image_tag("icon_red_heart.svg")
-#       %span
-#         = book.like_count
-#   - else
-#     = button_to book_likes_path(@book), id: "like-button", remote: true do
-#       = image_tag("icon_heart.svg")
-#       %span
-#         = book.like_user
-# - else
-#   = image_tag("icon_heart.svg")
-#   %span
-#     = book.like_count
